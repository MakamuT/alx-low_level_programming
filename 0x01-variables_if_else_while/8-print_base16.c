#include<stdio.h>
/**
 * main - function
 *
 * Return: always 0
 */
int main(void)
{
	char c;

	int d;

	c = 'a';
	d = '0';
	while
		(d < 0) {
			putchar(d + '0');
			d++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
