#include "main.h"
#include<stdio.h>
/**
 * _strlen - returns the length of a string
 * @s: pointer
 * Return: 0
 */

int _strlen(char *s)
{
	int c;

	for (c = 0 ; s[c] != 0 ; c++)
	{
		return (0);
	
	}
	return (0);
}
