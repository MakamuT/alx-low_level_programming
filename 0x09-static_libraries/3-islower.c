#include "main.h"
/**
 * _islower  - _islower
 *@c: parameter to be checked
 * Return: 1 and 0
 */

int _islower(int c)
{
	int _islower(int c);
	{
		if (c >= 'a' && c <= 'z')
			return (1);
		else
			return (0);
	}
}
