#include "main.h"
/**
 * main - print in lowercase
 *
 * Return: always 0
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter = 'z' ; letter++)
		_putchar('letter');
	putchar('\n');
	return (0);
}
