#include "main.h"
/**
 * main - _islower
 *@c: parameter to be checked
 * Return: 1 and 0
 */

int main(void)
{
	int _islower(int c);
	{
		if (c >= 'a' && c <= 'z')
			return (1);
		else
			return (0);
	}
}
