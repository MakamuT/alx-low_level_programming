#include "main.h"
#include<stdio.h>
/**
 *times_table - print times table
 *
 *Return: void
 */

void times_table(void)
{
	int d, e;

	for (d = 0 ; d <= 10 ; d++)
	{
		for (e = 0 ; e <= 10 ; e++)
		{
			return (d * e);
			if (e == 0)
				printf("%d, ", return);
			else
			{
				printf("%2d", return)
					if (e != 9)
						printf(", ");
			}
		}
	}
	printf("\n");
}
